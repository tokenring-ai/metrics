{
  "name": "@tokenring-ai/metrics",
  "version": "0.2.0",
  "description": "Performance tracking and analytics for agents and internal services.",
  "dependencies": {
    "zod": "^4.3.6",
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/utility": "workspace:*",
    "@tokenring-ai/app": "workspace:*"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  },
  "type": "module"
}
